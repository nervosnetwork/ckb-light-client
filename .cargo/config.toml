# Android cross-compilation configuration
# The actual toolchain paths and RocksDB flags are set by
# build-android-jni.sh / build-android.sh via environment variables.
# Do NOT set RocksDB env vars here — they would affect all targets
# including native desktop builds.

[target.aarch64-linux-android]
# Environment variables will set the linker and ar
