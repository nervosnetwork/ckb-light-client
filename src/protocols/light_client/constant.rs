use std::time::Duration;

pub const REFRESH_PEERS_TOKEN: u64 = 0;

pub const REFRESH_PEERS_DURATION: Duration = Duration::from_secs(60);

pub const REFRESH_PEERS_INTERVAL: Duration = Duration::from_secs(300);
